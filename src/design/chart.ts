import { color as C } from "./tokens";

/** Shared Recharts styling so every chart reads as one system. */
export const axisTick = { fill: C.text3, fontSize: 11, fontFamily: "Manrope Variable, Manrope, sans-serif" };
export const axisTickMono = { fill: C.text2, fontSize: 11, fontFamily: "JetBrains Mono Variable, monospace" };
export const gridStroke = "rgba(22,22,22,0.07)";
